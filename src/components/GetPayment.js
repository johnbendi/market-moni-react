import { CardBody, CardTitle, Form } from "reactstrap"

export function GetPayment(props) {
  return (
    <CardBody>
      <CardTitle>Get Payment</CardTitle>
      <Form></Form>
    </CardBody>
  )
}
